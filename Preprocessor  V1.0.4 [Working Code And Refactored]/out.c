#include <stdio.h>
void main()
{
int j = 2;
int motor,sensorValue = 0;
if(motor == 1
)//what to do when motor is running
{
sensorValue++;
}
else if(motor == OFF)/* what to do when motor is not running */
{
sensorVlaue--;
}
return 0;
}
